HomeCloud 是一个功能丰富、注重安全与用户体验的家庭级私有云盘，基于 Node.js + Express 构建，前端采用纯静态 HTML/CSS/JavaScript，无需复杂依赖即可快速部署。以下是它的核心特点：

安装方法：

1️⃣ 环境准备
Node.js 12.x 或更高版本（推荐 LTS）
下载地址：https://nodejs.org/
npm（通常随 Node.js 一起安装）

2️⃣安装依赖
在目录下打开cmd,运行：
npm install

3️⃣ 启动服务
运行：
node server.js
或者：
npm start

如果看到：
✅ HomeCloud 服务器运行中
➜ 本地: http://localhost:8080
➜ 内网: 请查看前端显示IP
➜ 管理员: root / root (密码已哈希)
就成功了

4️⃣ 访问云盘
本地访问：http://localhost:8080
局域网访问：查看前端页面显示的 IP（如 http://192.168.1.100:8080），确保防火墙允许该端口。
首次使用：点击“创建新帐户”注册普通用户，或直接使用管理员账户 root / root（强烈建议首次登录后修改密码）。

✨ 新增功能
修改用户名

现在普通用户可以自行修改用户名（root 账户除外）。

在设置界面「账号设置」中添加了“修改用户名”卡片，输入新用户名和当前密码即可完成修改。

修改后需重新登录，所有相关界面用户名实时更新。

关于页面

侧边栏新增“ⓘ 关于”按钮（位于设置按钮上方），点击即可打开关于模态框。

关于页面显示版本号、项目简介、技术栈、开源协议以及您的 GitHub 仓库链接。

设置界面右上角也保留了相同的关于按钮，方便快速访问。

🎨 界面美化
设置界面优化

调整了设置卡片的间距、圆角和阴影，视觉上更精致、现代。

为每个设置项添加了更丰富的图标，提升辨识度。

颜色预设区域和自定义颜色选择器布局更合理。

玻璃效果开关

修复了暗色模式下玻璃效果开关对比度不足的问题。

开关状态会保存到本地，刷新页面后保持用户偏好。

🐛 修复的 BUG
搜索模式退出后：面包屑导航未重置的问题已修复，现在退出搜索后能正确显示当前文件夹路径。

受保护文件夹后代文件：下载时错误提示不明确的问题已优化，现在会正确提示需要解锁密码。

密码解锁流程：修复了多次弹出相同密码框的问题，解锁流程更顺畅。

头像预览：修复了某些情况下头像预览区域显示错位的问题。

响应式适配：移动端侧边栏菜单交互优化，展开/收起更流畅。

🚀 其他改进
后端接口：新增 PUT /api/user/username 接口，支持用户名修改，并进行了密码验证和安全检查。

前端代码：重构了部分 JavaScript 逻辑，提升了性能和可维护性。

文档：更新了项目内相关注释，便于后续开发。

